name: frontend

on:
  pull_request:
  push:
    branches:
      - main
      - v2-underwriting-engine

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: Install frontend dependencies
        run: npm install
      - name: Build React dashboard
        run: npm run build
